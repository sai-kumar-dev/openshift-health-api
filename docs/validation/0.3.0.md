# Release validation: 0.3.0

Date: 2026-07-27

Platform: Windows, sandboxed workspace

Python: CPython 3.13.7

## Locally executed

| Check | Command | Result |
|---|---|---|
| Formatting | `ruff format --check .` | Passed |
| Lint | `ruff check .` | Passed |
| Types | `python -m mypy app tests` | Success; 10 source files |
| Tests | `python -m pytest --cov-report=term-missing --cov-report=xml` | 27 passed in 0.61 seconds |
| Branch coverage | pytest configuration | 96.07%; required 90% |
| Compilation | `python -m compileall -q app tests scripts` | Passed |
| Development manifests | `kubectl kustomize deploy/overlays/development` | Rendered |
| Production manifests | `kubectl kustomize deploy/overlays/production` | Rendered |

## Hosted validation

- [GitHub Actions run 30282610925](https://github.com/sai-kumar-dev/openshift-health-api/actions/runs/30282610925)
  passed on commit `809e3274e50e29357077eafcabb454b376a5edd1`.
- Python 3.11, 3.12, and 3.13 tests, Ruff, strict mypy, pip-audit, Gitleaks,
  Kustomize rendering, Trivy configuration scanning, container build/smoke, and
  the Trivy HIGH/CRITICAL image gate all passed.

## Local container validation

- Docker Desktop 29.2.0 built `openshift-health-api:0.3.0` successfully.
- The approximately 80 MB minimal UBI image ran as UID `1000710000:0`, and with a
  read-only root filesystem plus a 64 MiB `/tmp` tmpfs.
- All four smoke-tested endpoints returned HTTP 200.
- Image inspection found only the six application source/configuration files and
  `requirements.txt`; recursive bytecode-cache exclusions were verified after rebuild.

## Unavailable or not executed

- Trivy, Gitleaks, kubeconform, yamllint, and actionlint were not installed locally;
  the configured hosted checks above are the authoritative security evidence.
- Podman was not installed; Docker was the available runtime.
- No Kubernetes API, OpenShift CLI/context, CRC, or other local cluster was available.
- Quay publication and OpenShift deployment were not verified.

Rendering YAML only proves Kustomize composition. It does not prove API schema
acceptance, image pullability, admission-policy compatibility, or successful rollout.
